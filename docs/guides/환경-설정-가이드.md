# 환경 설정 가이드

React Native 개발을 시작하기 위한 환경 설정 가이드입니다.

## 필수 설치 항목

### 1. Node.js 설치

**권장 버전**: v18.x 이상

#### macOS
```bash
# Homebrew 사용
brew install node

# 또는 nvm 사용 (권장)
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash
nvm install 18
nvm use 18
```

#### Windows
- [Node.js 공식 사이트](https://nodejs.org/)에서 LTS 버전 다운로드

**설치 확인**
```bash
node -v  # v18.x.x
npm -v   # 9.x.x
```

### 2. Git 설치

#### macOS
```bash
brew install git
```

#### Windows
- [Git 공식 사이트](https://git-scm.com/)에서 다운로드

**설치 확인**
```bash
git --version
```

**Git 설정**
```bash
git config --global user.name "Your Name"
git config --global user.email "your.email@example.com"
```

### 3. 코드 에디터 설치

#### VS Code (권장)
- [VS Code 다운로드](https://code.visualstudio.com/)

**필수 확장 프로그램**
- ESLint
- Prettier - Code formatter
- React Native Tools
- TypeScript Vue Plugin (Volar)

**권장 확장 프로그램**
- Auto Rename Tag
- Bracket Pair Colorizer 2
- ES7+ React/Redux/React-Native snippets
- Path Intellisense

### 4. Expo CLI 설치

```bash
npm install -g expo-cli

# 또는 yarn 사용
yarn global add expo-cli
```

**설치 확인**
```bash
expo --version
```

### 5. Expo Go 앱 설치

실제 기기에서 테스트하기 위해 모바일 앱 설치

- **iOS**: App Store에서 "Expo Go" 검색
- **Android**: Play Store에서 "Expo Go" 검색

## 개발 환경 선택

### Option 1: Expo (권장 - 초보자용)

**장점**
- 환경 설정 간단
- 빠른 시작
- OTA(Over-The-Air) 업데이트
- 많은 기본 기능 제공

**단점**
- 네이티브 모듈 제한
- 앱 크기가 큼

### Option 2: React Native CLI (숙련자용)

**장점**
- 완전한 제어
- 네이티브 모듈 자유롭게 사용
- 앱 크기 최적화

**단점**
- 복잡한 설정
- Xcode/Android Studio 필요

**스터디에서는 Expo 사용을 권장합니다!**

## 프로젝트 시작하기

### 1. 새 프로젝트 생성

```bash
# 프로젝트 디렉토리 생성
npx create-expo-app my-first-app

# 디렉토리 이동
cd my-first-app
```

### 2. 프로젝트 실행

```bash
# 개발 서버 시작
npm start

# 또는
expo start
```

### 3. 앱 실행 방법

터미널에 QR 코드가 표시됩니다.

**iOS (iPhone)**
1. 카메라 앱으로 QR 코드 스캔
2. Expo Go 알림 클릭
3. 앱이 Expo Go에서 실행됨

**Android**
1. Expo Go 앱 실행
2. "Scan QR Code" 탭
3. QR 코드 스캔
4. 앱이 실행됨

**에뮬레이터/시뮬레이터**
- `i` 키: iOS 시뮬레이터
- `a` 키: Android 에뮬레이터

## iOS 시뮬레이터 설정 (macOS only)

### Xcode 설치

1. App Store에서 Xcode 설치 (무료, 약 10GB)
2. Xcode 실행 후 추가 컴포넌트 설치
3. Command Line Tools 설정
   ```bash
   sudo xcode-select --switch /Applications/Xcode.app/Contents/Developer
   ```

### 시뮬레이터 실행

```bash
# Expo 개발 서버 실행 후
# 터미널에서 'i' 키 입력
```

또는

```bash
# Xcode에서 직접 실행
open -a Simulator
```

## Android 에뮬레이터 설정

### Android Studio 설치

1. [Android Studio 다운로드](https://developer.android.com/studio)
2. 설치 및 실행
3. SDK Manager에서 필요한 SDK 설치
   - Android SDK Platform 33
   - Android SDK Build-Tools
   - Android Emulator

### 가상 기기 생성

1. Android Studio > Tools > AVD Manager
2. Create Virtual Device
3. Pixel 5 선택 (권장)
4. System Image: API Level 33 선택
5. Finish

### 환경 변수 설정

#### macOS/Linux
```bash
# ~/.zshrc 또는 ~/.bashrc에 추가
export ANDROID_HOME=$HOME/Library/Android/sdk
export PATH=$PATH:$ANDROID_HOME/emulator
export PATH=$PATH:$ANDROID_HOME/platform-tools
```

적용:
```bash
source ~/.zshrc
```

#### Windows
1. 시스템 환경 변수 설정
2. `ANDROID_HOME`: `C:\Users\[USERNAME]\AppData\Local\Android\Sdk`
3. Path에 추가:
   - `%ANDROID_HOME%\emulator`
   - `%ANDROID_HOME%\platform-tools`

### 에뮬레이터 실행

```bash
# Expo 개발 서버 실행 후
# 터미널에서 'a' 키 입력
```

## VS Code 설정

### settings.json 추천 설정

```json
{
  "editor.formatOnSave": true,
  "editor.defaultFormatter": "esbenp.prettier-vscode",
  "editor.codeActionsOnSave": {
    "source.fixAll.eslint": true
  },
  "typescript.updateImportsOnFileMove.enabled": "always",
  "javascript.updateImportsOnFileMove.enabled": "always"
}
```

### .prettierrc 생성

프로젝트 루트에 파일 생성:

```json
{
  "semi": true,
  "trailingComma": "es5",
  "singleQuote": true,
  "printWidth": 80,
  "tabWidth": 2
}
```

## TypeScript 설정 (선택)

Expo는 TypeScript를 기본 지원합니다.

```bash
# TypeScript로 프로젝트 생성
npx create-expo-app my-app --template expo-template-blank-typescript
```

기존 프로젝트에 추가:
```bash
npm install --save-dev typescript @types/react @types/react-native
```

## 유용한 도구

### React Native Debugger

Chrome DevTools보다 강력한 디버깅 도구

```bash
# macOS
brew install --cask react-native-debugger

# Windows
# GitHub Releases에서 다운로드
```

### Flipper (선택)

Meta에서 만든 디버깅 플랫폼
- [Flipper 다운로드](https://fbflipper.com/)

## 문제 해결

### 포트가 이미 사용 중인 경우

```bash
# 기본 포트(19000) 변경
expo start --port 19001
```

### Expo Go 연결 안 될 때

1. 컴퓨터와 모바일 기기가 같은 Wi-Fi에 연결되어 있는지 확인
2. 방화벽 설정 확인
3. Tunnel 모드 사용
   ```bash
   expo start --tunnel
   ```

### 시뮬레이터/에뮬레이터 느릴 때

- 컴퓨터 재시작
- 가상 기기 재생성
- RAM 할당량 증가 (Android)

### 캐시 문제

```bash
# Expo 캐시 클리어
expo start -c

# npm 캐시 클리어
npm cache clean --force
```

## 성능 최적화

### 개발 모드
```bash
# Fast Refresh 활성화 (기본값)
# 코드 변경 시 자동 새로고침
```

### 프로덕션 빌드
```bash
# 최적화된 빌드
expo build:android
expo build:ios
```

## 다음 단계

환경 설정이 완료되었다면:

1. [Week 1 학습 자료](../weekly-notes/week-01.md) 시작
2. 간단한 "Hello World" 앱 만들어보기
3. 공식 문서 튜토리얼 따라하기

## 참고 자료

- [Expo 공식 문서](https://docs.expo.dev/)
- [React Native 공식 문서](https://reactnative.dev/docs/environment-setup)
- [React Native 디버깅 가이드](https://reactnative.dev/docs/debugging)

## 도움이 필요하면

- GitHub Issues에 질문 올리기
- 스터디 미팅에서 함께 해결하기
- [Expo 디스코드](https://discord.gg/expo) 커뮤니티 활용

---

환경 설정 중 문제가 있으면 언제든 Issues에 올려주세요! 🚀
